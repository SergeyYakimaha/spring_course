spring.datasource.url = jdbc:postgresql://127.0.0.1:5432/my_db
spring.datasource.username = postgres
spring.datasource.password = admin

spring.jpa.show-sql = true

server.servlet.context-path = /spring-data-rest

logging.level.web = DEBUG
logging.level.sql = DEBUG

spring.data.rest.default-page-size = 20

info.name = Spring Data Rest
info.description = CRUD operations

#management.endpoints.web.exposure.include = beans, mapping
management.endpoints.web.exposure.include = *
